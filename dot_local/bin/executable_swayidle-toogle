#!/bin/sh
set -eu

if pgrep -x swayidle >/dev/null 2>&1; then
  pkill -x swayidle
  notify-send -a 'Idle' -i system-lock-screen -u low 'Idle' 'swayidle stopped'
else
  swayidle -w >/dev/null 2>&1 &
  notify-send -a 'Idle' -i system-lock-screen -u normal 'Idle' 'swayidle started'
fi
